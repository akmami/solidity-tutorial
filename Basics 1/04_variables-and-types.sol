// SPDX-License-Identifier: GPL-3.0

pragma solidity >= 0.8.2 < 0.9.0;

contract Variables {

    uint chocolateBar = 10;
    uint storeOwner = 300;

    bool lieDetector = true;

    string errorMessage = "Error! There has been a mistake! :(";

    uint wallet = 500;
    bool spent = false;
    string notifySpend = "you have spent modey";
}